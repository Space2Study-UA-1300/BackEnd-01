paths:
  /offers:
    post:
      tags:
        - Offers
      summary: Create offer
      description: Create offer with provided data
      produces:
        - application/json
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/createOfferRequest'
      responses:
        201:
          description: Users
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/offerResponse'
